#!/usr/bin/ruby 

def colorize(text, color_code)
  "\e[#{color_code}m#{text}\e[0m"
end

def red(text)
	colorize(text, 31)
end

def green(text)
	colorize(text, 32)
end

if ARGV[0].to_i == 0
	puts "#{green('Success')}"
else
	puts "#{red('Failed')}"
end